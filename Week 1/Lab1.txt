//
//  ContentView.swift
//  LabWeek1
//
//  Created by May06 on 14/9/26.
//

import SwiftUI

struct ContentView: View {
    var body: some View {
        ZStack {
            Image("Image2").resizable(capInsets: .init(top: 0, leading: 0, bottom: 0.3, trailing: 0)).frame(maxWidth: .infinity, maxHeight: .infinity)
            VStack {
                let StuName = "Khoi"
                let StuID = "SESEIU24013"
                var age = 20
                var gpa = 4
                var isStu = true
                ZStack {
                    Image("Images2")
                    VStack {
                        Spacer()
                        Text("🎓").font(.system(size: 100)).bold()
                        Text("Student Profile").font(.largeTitle).bold().foregroundStyle(.white)
                        Text("Name: \(StuName)").foregroundStyle(.white)
                        Text("Student ID: \(StuID)").foregroundStyle(.white)
                        Text("Age: \(age)").foregroundStyle(.white)
                        Text("GPA: \(gpa)").foregroundStyle(.white)
                        Text("Is Student: \(isStu)").foregroundStyle(.white)
                    }
                }
            }
            .padding()
        }
    }
}

#Preview {
    ContentView()
}
