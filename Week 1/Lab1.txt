//
//  ContentView.swift
//  LabWeek1
//
//  Created by May06 on 14/9/26.
//

import SwiftUI

struct ContentView: View {
    var body: some View {
            VStack {
                let Name = "Minh Tam"
                let ID = "SESEIU24023"
                var age = 20
                var gpa = 3
                var isStu = true
                ZStack {
                    Image("Images2")
                    VStack {
                        Spacer()
                        Text("🎓").font(.system(size: 100)).bold()
                        Text("Student Profile").font(.largeTitle).bold().foregroundStyle(.white)
                        Text("Name: \(Name)").foregroundStyle(.white)
                        Text("Student ID: \(ID)").foregroundStyle(.white)
                        Text("Age: \(age)").foregroundStyle(.white)
                        Text("GPA: \(gpa)").foregroundStyle(.white)
                        Text("Is Student: \(isStu)").foregroundStyle(.white)
                    }
                }
            }
            .padding()
    }
}

#Preview {
    ContentView()
}
